1) Create ssh private and public key
2) Put the name of the private the key into ansible.cfg and to $HOME/.ssh/ 
3) Define private and public ssh key in terraform/variables.tf
4) Run aws configure to defin access and secret key
5) Execute:
   - terraform init && terraform apply -auto-approve
